[pytest]
pythonpath = . whatsapp_service orchestrator_service tiendanube_service
asyncio_mode = auto
testpaths = tests
# Run only working tests (orchestrator + whatsapp need service fixes)
python_files = test_tiendanube.py
