server {
    listen 80;
    server_name localhost;

    # API Proxy removed in favor of Logic-based routing (Protocol Omega)
    # The frontend app.js dynamically detects the API_BASE URL.


    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}